import React from 'react';

export const ProgramBlockTransition: React.FC<{ blockNumber: number; onClose: () => void; lang: 'en' | 'es' }> = ({ blockNumber, onClose, lang }) => {
  const isSecond = blockNumber === 2;
  return <div className="fixed inset-0 z-modal flex items-end bg-black/70 p-4 backdrop-blur-sm sm:items-center"><div className="mx-auto w-full max-w-md rounded-3xl border border-primary-500/30 bg-[#111114] p-6 text-white shadow-2xl"><p className="text-xs font-black uppercase tracking-[0.2em] text-primary-400">KONG</p><h1 className="mt-2 text-2xl font-black">{lang === 'es' ? `BLOCK ${blockNumber} · ${isSecond ? 'PYRAMIDS + FATIGUED STRENGTH' : 'OVERLOAD + REVERSE PYRAMIDS'}` : `BLOCK ${blockNumber} · ${isSecond ? 'PYRAMIDS + FATIGUED STRENGTH' : 'OVERLOAD + REVERSE PYRAMIDS'}`}</h1><p className="mt-3 text-sm leading-6 text-zinc-400">{isSecond ? (lang === 'es' ? 'Capacidad construida. Los compounds vuelven al frente; todas las series de la pirámide cuentan como trabajo.' : 'Capacity built. Compounds return to the front; every pyramid set counts as work.') : (lang === 'es' ? 'Ahora prioriza peso pesado estando fresco. El top set va primero y los backoffs de altas reps siguen siendo importantes.' : 'Now prioritize heavy weight while fresh. The top set comes first and high-rep backoffs still matter.')}</p><button onClick={onClose} className="mt-6 min-h-12 w-full rounded-2xl bg-primary-500 font-black text-black">{lang === 'es' ? 'Continuar' : 'Continue'}</button></div></div>;
};
